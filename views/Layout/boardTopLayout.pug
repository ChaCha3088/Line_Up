doctype html
html(lang='ko')
    head
        title #{pageTitle}
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        link(rel='stylesheet', href='/StyleSheets/boardLayout.css')

    block header